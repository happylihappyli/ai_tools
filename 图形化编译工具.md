# 图形化编译工具

这个工具可以显示编译总耗时，进度条。
编译的提示信息，而且可以把编译结果保存到某个文件中。
方便AI查看，也方便人类查看。
用户可以在工具中查看编译的详细信息，包括编译的文件，编译的选项，编译的结果等。
用qt实现界面。

**【版本开发规范】**
- **强烈推荐用户使用 C++ 版本，而不是 Python 版本。**
- 尽管 Python 可以方便地启动、修改配置、配合AI工具使用，但作为核心要求，**Python 中有的功能都要在 C++ 中实现**。C++ 版本需要具备完全等同于甚至超越 Python 版本的能力。
